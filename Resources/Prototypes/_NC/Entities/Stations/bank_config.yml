- type: entity
  id: NCStationBank
  parent: BaseStationCargo
  name: station bank settings
  components:
  - type: StationBank
    accounts:
      CityAdmin:
        balance: 0
        # increasePerSecond: 0  # This parameter is not used in StationBank, only in StationBankAccount
      TraumaTeam:
        balance: 10000
        # increasePerSecond: 5  # This parameter is not used in StationBank, only in StationBankAccount
      Militech:
        balance: 25000
        # increasePerSecond: 8  # This parameter is not used in StationBank, only in StationBankAccount
      Biotechnica:
        balance: 15000
        # increasePerSecond: 6  # This parameter is not used in StationBank, only in StationBankAccount
      Ncpd:
        balance: 10000
        # increasePerSecond: 0  # This parameter is not used in StationBank, only in StationBankAccount
